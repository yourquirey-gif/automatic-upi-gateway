(()=>{
const API='https://api.omniupi.in/api/v1';
let lastKey='';
const token=()=>localStorage.getItem('gateway_access_token');
async function req(path,opts={}){const t=token();if(!t)throw new Error('Not logged in');const r=await fetch(API+path,{...opts,headers:{Accept:'application/json','Content-Type':'application/json',Authorization:`Bearer ${t}`,...(opts.headers||{})}});const d=await r.json().catch(()=>({}));if(!r.ok||d.status===false)throw new Error(d.message||`Request failed (${r.status})`);return d}
function esc(v){return String(v??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[c]))}
function render(account,merchants){const root=document.querySelector('.dash-main');if(!root)return;let box=document.getElementById('omni-upi-sync');if(!box){box=document.createElement('section');box.id='omni-upi-sync';box.style.cssText='margin:0 0 18px;padding:18px;border:1px solid #e7eaf0;border-radius:20px;background:#fff;box-shadow:0 10px 30px rgba(20,30,50,.06);position:relative;z-index:5';root.prepend(box)}const admin=account?.role==='admin';const rows=(merchants||[]).map(m=>{const verified=m.verificationStatus==='verified';return `<div data-mid="${esc(m._id)}" style="display:flex;align-items:center;justify-content:space-between;gap:12px;padding:12px 0;border-top:1px solid #eef1f5"><div><b>${esc(m.name||'UPI Merchant')}</b><div style="font-size:12px;color:#7b8492;margin-top:3px">${esc(m.upiId||'—')} · ${esc(m.provider||'UPI')}</div></div><div style="display:flex;align-items:center;gap:8px"><span style="padding:6px 10px;border-radius:999px;font-size:11px;font-weight:800;background:${verified?'#ecfbf3':'#fff7df'};color:${verified?'#15734f':'#856a13'}">${verified?'✓ Verified':'Verification required'}</span><button data-delete="${esc(m._id)}" style="border:1px solid #f0b7b1;background:#fff;border-radius:9px;padding:7px 10px;color:#b42318;font-weight:700;cursor:pointer">Delete</button></div></div>`}).join('');const adminMerchant=(merchants||[]).find(m=>m.provider==='admin_settlement');const adminText=adminMerchant?(adminMerchant.verificationStatus==='verified'?`<span style="color:#15734f">✓ Admin payment UPI is already verified.</span>`:`<span style="color:#856a13">Admin payment UPI needs Google/Gmail verification.</span>`):'No admin settlement UPI configured.';box.innerHTML=`<div style="display:flex;justify-content:space-between;gap:12px;align-items:flex-start"><div><div style="font-size:16px;font-weight:850">UPI Verification Status</div><div style="font-size:12px;color:#7b8492;margin-top:4px">${admin?adminText:'Your connected UPI accounts and Gmail payment verification status.'}</div></div><button id="omni-upi-refresh" style="border:1px solid #dfe4ec;background:#fff;border-radius:9px;padding:7px 10px;cursor:pointer">Refresh</button></div>${rows||'<div style="padding:14px 0;color:#7b8492;font-size:12px">No connected UPI merchant found. Add a merchant from Connect Merchant.</div>'}`;box.querySelector('#omni-upi-refresh')?.addEventListener('click',load);box.querySelectorAll('[data-delete]').forEach(btn=>btn.addEventListener('click',async()=>{const id=btn.dataset.delete;if(!confirm('Delete this UPI merchant and its Gmail verification connection?'))return;btn.disabled=true;try{await req(`/merchants/${encodeURIComponent(id)}`,{method:'DELETE'});await load();}catch(e){btn.disabled=false;alert(e.message)}}))}
async function load(){if(!token()||!location.hash.includes('dashboard'))return;try{const [a,m]=await Promise.all([req('/account'),req('/merchants')]);const account=a.user||{};const merchants=m.merchants||[];const key=JSON.stringify({id:account._id,role:account.role,ms:merchants.map(x=>[x._id,x.verificationStatus,x.status,x.upiId])});if(key!==lastKey){lastKey=key;render(account,merchants)}}catch{}}
setInterval(load,4000);window.addEventListener('hashchange',()=>setTimeout(load,300));setTimeout(load,1200);
})();
