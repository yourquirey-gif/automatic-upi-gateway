const API_BASE='https://automatic-upi-gateway.onrender.com';
const token=localStorage.getItem('autogateway_token');
if(!token){location.href='./index.html?login=required'}
const app=document.getElementById('app'), crumb=document.getElementById('crumb'), sidebar=document.getElementById('sidebar');
const nav=[...document.querySelectorAll('#nav button')];
const state={user:null,account:null,merchants:[],orders:[],plans:[],tickets:[],api:null};
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const money=n=>'₹'+Number(n||0).toLocaleString('en-IN',{minimumFractionDigits:2,maximumFractionDigits:2});
function toast(msg){const t=document.getElementById('toast');t.textContent=msg;t.classList.add('show');clearTimeout(window.__toast);window.__toast=setTimeout(()=>t.classList.remove('show'),2800)}
async function api(path,opts={}){const headers={'Accept':'application/json','Content-Type':'application/json',...(opts.headers||{}),Authorization:`Bearer ${token}`};const r=await fetch(API_BASE+path,{...opts,headers});let d={};try{d=await r.json()}catch{}if(!r.ok||d.status===false)throw new Error(d.message||`Request failed (${r.status})`);return d}
async function loadAccount(){const d=await api('/api/v1/account');state.user=d.user;state.account=d;document.getElementById('account').textContent=d.user?.email||'Authenticated';document.getElementById('avatar').textContent=(d.user?.name||d.user?.email||'M')[0].toUpperCase();return d}
async function loadMerchants(){const d=await api('/api/v1/merchants');state.merchants=d.merchants||[];return state.merchants}
async function loadOrders(){const d=await api('/api/v1/orders?limit=100');state.orders=d.orders||[];return state.orders}
async function loadPlans(){const d=await api('/api/v1/subscriptions/plans');state.plans=d.plans||[];return state.plans}
function fmtDate(v){if(!v)return '—';try{return new Date(v).toLocaleString('en-IN',{dateStyle:'medium',timeStyle:'short'})}catch{return String(v)}}
function statusBadge(s){const x=String(s||'').toLowerCase();return `<span class="badge ${x==='success'?'success':x==='pending'?'pending':x==='failed'?'failed':x==='expired'?'expired':'pending'}">${esc(s||'UNKNOWN')}</span>`}
function empty(title,text='Nothing to show yet.'){return `<div class="empty-state"><div><strong>${esc(title)}</strong><span>${esc(text)}</span></div></div>`}
function loading(){app.innerHTML='<div class="loading">Loading your account…</div>'}
function show(page){nav.forEach(b=>b.classList.toggle('active',b.dataset.page===page));crumb.textContent=page;sidebar.classList.remove('open');window.scrollTo({top:0,behavior:'smooth'});const views={Dashboard:dashboard,'Connect Merchant':merchants,'Payment Link':paymentLinks,Transactions:transactions,'Edit Checkout Page':checkout,Subscription:subscription,'Account & IP Whitelist':accountPage,'Change Password':passwordPage,'API Details':apiDetails,'Merchant APK':apkPage,'Video For Merchant':videoPage,Documentation:docsPage,'Support Ticket':supportPage,FAQ:faqPage,'SDK File':sdkPage,Security:security};if(views[page]){loading();Promise.resolve(views[page]()).catch(e=>app.innerHTML=`<div class="panel"><h2>Unable to load this page</h2><p class="subtext">${esc(e.message)}</p></div>`)} }
nav.forEach(b=>b.onclick=()=>show(b.dataset.page));document.getElementById('menu').onclick=()=>sidebar.classList.toggle('open');document.getElementById('avatar').onclick=()=>show('Account & IP Whitelist');
async function dashboard(){await Promise.all([loadAccount(),loadMerchants(),loadOrders()]);const o=state.orders;const success=o.filter(x=>x.status==='SUCCESS');const pending=o.filter(x=>x.status==='PENDING');const failed=o.filter(x=>['FAILED','EXPIRED'].includes(x.status));const received=success.reduce((a,x)=>a+Number(x.amount||0),0);const rate=o.length?((success.length/o.length)*100).toFixed(1):'0.0';const recent=o.slice(0,6);app.innerHTML=`<div class="hero"><div><div class="eyebrow">MERCHANT DASHBOARD</div><h1>Welcome back, ${esc(state.user?.name||'Merchant')} 👋</h1><p>Manage payments, merchants and your account from one secure workspace.</p></div><button class="primary" onclick="show('Connect Merchant')">＋ Connect Merchant</button></div><div class="stats"><div class="stat green"><div class="stat-icon">₹</div><span>Today Received</span><strong>${money(received)}</strong><small>${success.length} successful transaction${success.length===1?'':'s'}</small></div><div class="stat blue"><div class="stat-icon">✓</div><span>Success Transactions</span><strong>${success.length}</strong><small>${rate}% success rate</small></div><div class="stat yellow"><div class="stat-icon">◷</div><span>Pending Payment</span><strong>${money(pending.reduce((a,x)=>a+Number(x.amount||0),0))}</strong><small>${pending.length} pending</small></div><div class="stat pink"><div class="stat-icon">×</div><span>Failed Payment</span><strong>${money(failed.reduce((a,x)=>a+Number(x.amount||0),0))}</strong><small>${failed.length} failed/expired</small></div></div><div class="grid"><div class="panel"><div class="panel-head"><div><h2>Transaction overview</h2><p>Recent gateway activity</p></div>${statusBadge('LIVE')}</div><div class="chart"><div class="chart-line"></div></div></div><div class="panel"><div class="panel-head"><div><h2>Gateway status</h2><p>Live account health</p></div></div><div class="health"><span class="health-dot big"></span><div><b>All systems operational</b><small>API, authentication and payment services</small></div></div><div class="health-row"><span>Connected merchants</span><b>${state.merchants.length}</b></div><div class="health-row"><span>API service</span><b>Online</b></div><div class="health-row"><span>Authentication</span><b>Active</b></div></div></div><div class="panel" style="margin-top:18px"><div class="panel-head"><div><h2>Recent Transactions</h2><p>Your latest payment activity.</p></div><button class="secondary" onclick="show('Transactions')">View All →</button></div>${recent.length?recent.map(x=>`<div class="txn-row"><span>${esc(x.orderId)}</span><b>${money(x.amount)}</b>${statusBadge(x.status)}<small>${fmtDate(x.createdAt)}</small></div>`).join(''):empty('No transactions found','Your latest payments will appear here.')}</div>`;updatePlan()}
async function merchants(){await loadMerchants();app.innerHTML=`<div class="hero"><div><div class="eyebrow">MERCHANT SETTING</div><h1>Connect Merchant</h1><p>Add and manage the UPI merchant accounts used for payment collection.</p></div><button class="primary" onclick="openMerchantModal()">＋ Add Merchant</button></div><div class="table-panel"><div class="panel-head"><div><h2>Connected merchants</h2><p>${state.merchants.length} merchant account${state.merchants.length===1?'':'s'} configured</p></div><button class="secondary" onclick="loadAndShowMerchants()">Refresh</button></div>${state.merchants.length?state.merchants.map((m,i)=>`<div class="merchant-row"><div class="merchant-avatar">${esc((m.name||'M')[0].toUpperCase())}</div><div><b>${esc(m.name)}</b><small>${esc(m.provider||'UPI')} · ${esc(m.upiId||'UPI ID not set')} · ${esc(m.mobile||'')}</small></div>${statusBadge(m.status||'active')}<button class="secondary" onclick="merchantInfo(${i})">Manage</button></div>`).join(''):empty('No merchant connected','Add your first merchant account to create payment links.')}</div>`}
async function loadAndShowMerchants(){await loadMerchants();show('Connect Merchant')}
function openMerchantModal(){document.body.insertAdjacentHTML('beforeend',`<div class="modal show" id="merchantModal"><div class="modal-card"><button class="modal-close" onclick="closeModal('merchantModal')">×</button><div class="eyebrow">CONNECT MERCHANT</div><h2>Add Merchant</h2><p class="modal-sub">Enter the merchant details used for UPI payment collection.</p><div class="form-grid"><div class="field full"><label>Merchant / Business Name</label><input id="mName" placeholder="Business name"></div><div class="field"><label>Provider</label><select id="mProvider"><option value="Paytm Business">Paytm Business</option><option value="BharatPe Merchant">BharatPe Merchant</option><option value="Google Pay Business">Google Pay Business</option><option value="PhonePe Business">PhonePe Business</option><option value="FamPay wallet">FamPay wallet</option><option value="Slice BANK">Slice BANK</option><option value="Digikhata">Digikhata</option><option value="NetBanking - Direct UPI ID">NetBanking - Direct UPI ID</option><option value="UPI - Direct UPI ID">UPI - Direct UPI ID</option></select></div><div class="field"><label>Registered Mobile</label><input id="mMobile" inputmode="numeric" maxlength="10" placeholder="10-digit mobile"></div><div class="field full"><label>UPI ID</label><input id="mUpi" placeholder="merchant@upi"></div><div class="field full"><label>External Merchant ID <span class="muted">(optional)</span></label><input id="mExternal" placeholder="Provider merchant ID"></div></div><div class="actions"><button class="primary" onclick="saveMerchant()">Save Merchant</button><button class="secondary" onclick="closeModal('merchantModal')">Cancel</button></div></div></div>`)}
async function saveMerchant(){const name=document.getElementById('mName').value.trim(),provider=document.getElementById('mProvider').value,mobile=document.getElementById('mMobile').value.replace(/\D/g,''),upiId=document.getElementById('mUpi').value.trim(),externalMerchantId=document.getElementById('mExternal').value.trim();if(!name||!provider)return toast('Merchant name and provider are required.');if(mobile&&mobile.length!==10)return toast('Enter a valid 10-digit mobile number.');try{await api('/api/v1/merchants',{method:'POST',body:JSON.stringify({name,provider,upiId,mobile,externalMerchantId})});closeModal('merchantModal');toast('Merchant connected successfully.');show('Connect Merchant')}catch(e){toast(e.message)}}
function merchantInfo(i){const m=state.merchants[i];openInfoModal('Merchant Details',`<div class="profile-grid"><div class="field"><label>Name</label><div class="input-like">${esc(m.name)}</div></div><div class="field"><label>Provider</label><div class="input-like">${esc(m.provider||'—')}</div></div><div class="field"><label>UPI ID</label><div class="input-like">${esc(m.upiId||'—')}</div></div><div class="field"><label>Mobile</label><div class="input-like">${esc(m.mobile||'—')}</div></div></div><div class="actions"><button class="secondary" onclick="closeModal('infoModal')">Close</button></div>`)}
async function paymentLinks(){await loadMerchants();await loadOrders();const links=state.orders.filter(x=>x.paymentUrl);app.innerHTML=`<div class="hero"><div><div class="eyebrow">CHECKOUT TOOLS</div><h1>Payment Link</h1><p>Create a real payment checkout URL from your connected UPI merchant.</p></div><button class="primary" onclick="openLinkModal()">＋ Create Link</button></div><div class="forms-grid"><div class="form-card"><h2>Create payment link</h2><p class="subtext">Create an order through your authenticated API credentials. The generated URL can be shared with a customer.</p><div class="notice" style="margin-top:14px">A connected active merchant is required. The customer is sent to the gateway checkout page for UPI payment.</div><div class="actions"><button class="primary" onclick="openLinkModal()">Create Payment Link</button><button class="secondary" onclick="show('Connect Merchant')">Manage Merchants</button></div></div><div class="info-card"><h2>Link security</h2><p class="subtext">Each link uses a unique order ID and is checked by the backend before payment status is returned.</p><div class="pill-row" style="margin-top:14px"><span class="pill">Unique Order ID</span><span class="pill">UPI</span><span class="pill">Server Verified</span></div></div></div><div class="table-panel" style="margin-top:18px"><div class="panel-head"><div><h2>Created payment links</h2><p>${links.length} generated link${links.length===1?'':'s'}</p></div><button class="secondary" onclick="show('Payment Link')">Refresh</button></div>${links.length?links.map(x=>`<div class="link-card"><div><b>${esc(x.remark1||x.orderId)}</b><small>${esc(x.orderId)} · ${money(x.amount)} · ${fmtDate(x.createdAt)}</small></div><code>${esc(x.paymentUrl)}</code><button class="secondary copy" onclick="copyText(${JSON.stringify(x.paymentUrl)})">Copy</button></div>`).join(''):empty('No payment links created','Create your first link using the button above.')}</div>`}
function openLinkModal(){if(!state.merchants.length){toast('Connect a merchant first.');show('Connect Merchant');return}document.body.insertAdjacentHTML('beforeend',`<div class="modal show" id="linkModal"><div class="modal-card"><button class="modal-close" onclick="closeModal('linkModal')">×</button><div class="eyebrow">CREATE PAYMENT LINK</div><h2>New Payment Link</h2><p class="modal-sub">The backend will create a real payment order and return its checkout URL.</p><div class="form-grid"><div class="field"><label>Amount (₹)</label><input id="lAmount" type="number" min="1" step="0.01" placeholder="999.00"></div><div class="field"><label>Customer Mobile <span class="muted">optional</span></label><input id="lMobile" inputmode="numeric" maxlength="10" placeholder="10-digit mobile"></div><div class="field full"><label>Merchant</label><select id="lMerchant">${state.merchants.map(m=>`<option value="${esc(m._id)}">${esc(m.name)} — ${esc(m.upiId||m.provider||'UPI')}</option>`).join('')}</select></div><div class="field"><label>Remark</label><input id="lRemark1" maxlength="200" placeholder="Order / product name"></div><div class="field"><label>Reference</label><input id="lRemark2" maxlength="200" placeholder="Optional reference"></div><div class="field full"><label>Redirect URL <span class="muted">optional</span></label><input id="lRedirect" placeholder="https://your-site.com/success"></div></div><div class="actions"><button class="primary" onclick="createRealLink()">Create Link</button><button class="secondary" onclick="closeModal('linkModal')">Cancel</button></div></div></div>`)}
async function createRealLink(){const amount=Number(document.getElementById('lAmount').value),customer_mobile=document.getElementById('lMobile').value.replace(/\D/g,''),merchant_id=document.getElementById('lMerchant').value,remark1=document.getElementById('lRemark1').value.trim(),remark2=document.getElementById('lRemark2').value.trim(),redirect_url=document.getElementById('lRedirect').value.trim();if(!amount||amount<=0)return toast('Enter a valid amount.');if(customer_mobile&&customer_mobile.length!==10)return toast('Customer mobile must be 10 digits.');try{const d=await api('/api/create-order',{method:'POST',body:JSON.stringify({amount,customer_mobile,merchant_id,remark1,remark2,redirect_url})});closeModal('linkModal');await copyText(d.result.paymentUrl);toast('Payment link created and copied.');show('Payment Link')}catch(e){toast(e.message)}}
async function transactions(){await loadOrders();const o=state.orders;app.innerHTML=`<div class="hero"><div><div class="eyebrow">PAYMENT ACTIVITY</div><h1>Transactions</h1><p>Track payment status, amount, UTR and transaction time.</p></div><button class="secondary" onclick="show('Transactions')">Refresh</button></div><div class="table-panel"><div class="panel-head"><div><h2>Transaction history</h2><p>${o.length} transaction${o.length===1?'':'s'} loaded</p></div><div class="table-tools"><select id="statusFilter" onchange="filterTx()"><option value="">All Status</option><option>PENDING</option><option>SUCCESS</option><option>FAILED</option><option>EXPIRED</option></select></div></div><div id="txBody">${txRows(o)}</div></div>`}
function txRows(o){return o.length?o.map(x=>`<div class="txn-row"><span>${esc(x.orderId)}</span><b>${money(x.amount)}</b>${statusBadge(x.status)}<small>${fmtDate(x.createdAt)}</small></div>`).join(''):empty('No transactions found','Transactions will appear here after the first order.')}
function filterTx(){const v=document.getElementById('statusFilter').value;document.getElementById('txBody').innerHTML=txRows(v?state.orders.filter(x=>x.status===v):state.orders)}
async function checkout(){const saved=JSON.parse(localStorage.getItem('checkout_settings')||'{}');app.innerHTML=`<div class="hero"><div><div class="eyebrow">MERCHANT SETTING</div><h1>Edit Checkout Page</h1><p>Customize the customer-facing checkout information used by your gateway.</p></div><button class="primary" onclick="saveCheckout()">Save Changes</button></div><div class="forms-grid"><div class="form-card"><h2>Checkout settings</h2><div class="form-grid"><div class="field full"><label>Business / Store Name</label><input id="cName" value="${esc(saved.name||state.user?.companyName||state.user?.name||'AutoGateway Merchant')}"></div><div class="field"><label>Support Email</label><input id="cEmail" type="email" value="${esc(saved.email||state.user?.email||'')}"></div><div class="field"><label>Support Mobile</label><input id="cMobile" value="${esc(saved.mobile||state.user?.mobile||'')}"></div><div class="field full"><label>Checkout Message</label><textarea id="cMessage">${esc(saved.message||'Secure UPI payment. Your payment status is verified automatically.')}</textarea></div></div></div><div class="info-card"><h2>Checkout preview</h2><div style="border:1px solid #e8eaf0;border-radius:13px;padding:20px;margin-top:13px"><b id="previewName">${esc(saved.name||'AutoGateway Merchant')}</b><p class="subtext" id="previewMessage" style="margin-top:8px">${esc(saved.message||'Secure UPI payment. Your payment status is verified automatically.')}</p><div class="input-like" style="margin-top:15px">Amount will be supplied by the payment order.</div><button class="primary" style="width:100%;margin-top:12px">Pay with UPI</button></div></div></div>`;['cName','cMessage'].forEach(id=>document.getElementById(id).addEventListener('input',()=>{document.getElementById(id==='cName'?'previewName':'previewMessage').textContent=document.getElementById(id).value}))}
function saveCheckout(){const d={name:document.getElementById('cName').value.trim(),email:document.getElementById('cEmail').value.trim(),mobile:document.getElementById('cMobile').value.trim(),message:document.getElementById('cMessage').value.trim()};localStorage.setItem('checkout_settings',JSON.stringify(d));toast('Checkout settings saved.')} 
async function subscription(){await loadPlans();let me={};try{me=await api('/api/v1/subscriptions/me')}catch{}app.innerHTML=`<div class="hero"><div><div class="eyebrow">ACCOUNT PLAN</div><h1>Subscription</h1><p>Manage your AutoGateway plan and access period.</p></div></div><div class="panel" style="margin-bottom:18px"><div class="panel-head"><div><h2>Current plan</h2><p>${me.active?'Your plan is active.':'No active paid plan is currently attached to your account.'}</p></div>${statusBadge(me.active?'ACTIVE':me.expired?'EXPIRED':'TRIAL')}</div><div class="pill-row"><span class="pill">${esc(me.plan?.name||'2-day Free Trial')}</span><span class="pill">Started: ${esc(fmtDate(me.startedAt))}</span><span class="pill">Expires: ${esc(fmtDate(me.expiresAt))}</span></div></div><div class="plans">${state.plans.length?state.plans.map((p,i)=>`<div class="plan-card ${i===1?'featured':''}">${i===1?'<span class="popular">POPULAR</span>':''}<h3>${esc(p.name)}</h3><div class="plan-price">${money(p.price)} <small>/ plan</small></div><ul><li>${esc(p.durationDays)} days access</li><li>UPI payment gateway</li><li>Transaction analytics</li><li>Merchant connection</li></ul><button class="primary" style="width:100%" onclick="buyPlan(${JSON.stringify(p._id)})">Choose ${esc(p.name)}</button></div>`).join(''):empty('No active plans','Ask the administrator to publish subscription plans.')}</div>`}
async function buyPlan(id){try{const d=await api('/api/v1/subscriptions/purchase',{method:'POST',body:JSON.stringify({planId:id})});openInfoModal('Subscription Payment',`<p class="subtext">Order <b>${esc(d.order.orderId)}</b> has been created for ${money(d.order.amount)}.</p><div class="notice" style="margin-top:13px">Tap the button below to open the UPI payment app.</div><div class="actions"><a class="primary" href="${esc(d.order.paymentUrl)}" style="text-decoration:none">Open UPI Payment</a><button class="secondary" onclick="closeModal('infoModal')">Close</button></div>`)}catch(e){toast(e.message)}}
async function accountPage(){const d=state.account||await loadAccount();const u=d.user||{};app.innerHTML=`<div class="hero"><div><div class="eyebrow">ACCOUNT SETTING</div><h1>Account & IP Whitelist</h1><p>Manage your merchant profile and approved server IP addresses.</p></div><button class="primary" onclick="saveAccount()">Save Changes</button></div><div class="forms-grid"><div class="form-card"><h2>Business profile</h2><div class="form-grid"><div class="field"><label>Full Name</label><input id="aName" value="${esc(u.name||'')}"></div><div class="field"><label>Mobile</label><input id="aMobile" value="${esc(u.mobile||'')}"></div><div class="field"><label>Company Name</label><input id="aCompany" value="${esc(u.companyName||'')}"></div><div class="field"><label>Email</label><input value="${esc(u.email||'')}" disabled></div><div class="field"><label>PAN Number</label><input id="aPan" value="${esc(u.panNumber||'')}"></div><div class="field"><label>Aadhaar Number</label><input id="aAadhaar" value="${esc(u.aadhaarNumber||'')}"></div><div class="field full"><label>Location</label><input id="aLocation" value="${esc(u.location||'')}"></div><div class="field full"><label>Whitelisted IPs <span class="muted">one per line</span></label><textarea id="aIps">${esc((u.whitelistedIps||[]).join('\n'))}</textarea></div></div></div><div class="info-card"><h2>Account status</h2><div class="health" style="margin-top:14px"><span class="health-dot big"></span><div><b>${esc(u.status||'active')}</b><small>Authenticated merchant account</small></div></div><div class="health-row"><span>User ID</span><b>${esc(u.userId||'—')}</b></div><div class="health-row"><span>Role</span><b>${esc(u.role||'merchant')}</b></div><div class="health-row"><span>KYC</span><b>${d.settings?.kycEnabled?'Enabled':'Not enabled'}</b></div></div></div>`}
async function saveAccount(){const body={name:document.getElementById('aName').value.trim(),mobile:document.getElementById('aMobile').value.trim(),companyName:document.getElementById('aCompany').value.trim(),panNumber:document.getElementById('aPan').value.trim(),aadhaarNumber:document.getElementById('aAadhaar').value.trim(),location:document.getElementById('aLocation').value.trim(),whitelistedIps:document.getElementById('aIps').value.split(/\r?\n/).map(x=>x.trim()).filter(Boolean)};try{const d=await api('/api/v1/account',{method:'PUT',body:JSON.stringify(body)});state.user=d.user;toast('Account updated successfully.')}catch(e){toast(e.message)}}
function passwordPage(){app.innerHTML=`<div class="hero"><div><div class="eyebrow">ACCOUNT SETTING</div><h1>Change Password</h1><p>Update your dashboard password securely.</p></div></div><div class="form-card" style="max-width:650px"><h2>Update password</h2><div class="form-grid" style="margin-top:15px"><div class="field full"><label>Current Password</label><input id="oldPass" type="password" autocomplete="current-password"></div><div class="field"><label>New Password</label><input id="newPass" type="password" minlength="8" autocomplete="new-password"></div><div class="field"><label>Confirm New Password</label><input id="newPass2" type="password" minlength="8" autocomplete="new-password"></div></div><div class="actions"><button class="primary" onclick="changePassword()">Change Password</button></div></div>`}
async function changePassword(){const currentPassword=document.getElementById('oldPass').value,newPassword=document.getElementById('newPass').value,confirm=document.getElementById('newPass2').value;if(newPassword!==confirm)return toast('New passwords do not match.');try{await api('/api/v1/account/password',{method:'PUT',body:JSON.stringify({currentPassword,newPassword})});toast('Password changed successfully.');document.getElementById('oldPass').value='';document.getElementById('newPass').value='';document.getElementById('newPass2').value=''}catch(e){toast(e.message)}}
async function apiDetails(){const d=await api('/api/v1/account/api');state.api=d.credentials;app.innerHTML=`<div class="hero"><div><div class="eyebrow">DEVELOPER SETTINGS</div><h1>API Details</h1><p>Use these credentials on your server to create orders and check payment status.</p></div><button class="primary" onclick="loadApiDetails()">Refresh</button></div><div class="settings-grid"><div class="form-card"><h2>API credentials</h2><p class="subtext">Keep credentials private. Never publish them in frontend source code.</p><div class="field" style="margin-top:15px"><label>User ID</label><div class="input-like">${esc(d.credentials.userId||'—')}</div></div><div class="secret"><code id="apiToken">${esc(d.credentials.apiToken)}</code><button onclick="copyText(${JSON.stringify(d.credentials.apiToken)})">Copy</button></div><div class="actions"><button class="secondary" onclick="regenerateCredential('token')">Regenerate API Token</button></div><div class="secret"><code>${esc(d.credentials.instanceSecret)}</code><button onclick="copyText(${JSON.stringify(d.credentials.instanceSecret)})">Copy</button></div><div class="actions"><button class="secondary" onclick="regenerateCredential('secret')">Regenerate Secret</button></div></div><div class="form-card"><h2>Webhook endpoint</h2><p class="subtext">Payment events can be delivered to your HTTPS endpoint after backend verification.</p><div class="field" style="margin-top:15px"><label>Webhook URL</label><input id="webhookUrl" value="${esc(d.credentials.webhookUrl||'')}" placeholder="https://your-domain.com/api/webhook"></div><div class="actions"><button class="primary" onclick="saveWebhook()">Save Endpoint</button></div><div class="notice" style="margin-top:15px">Webhook signatures use HMAC-SHA256 on the backend. Keep your instance secret private.</div></div></div>`}
async function loadApiDetails(){show('API Details')}
async function saveWebhook(){const webhookUrl=document.getElementById('webhookUrl').value.trim();try{await api('/api/v1/account/api/webhook',{method:'PUT',body:JSON.stringify({webhookUrl})});toast('Webhook endpoint saved successfully.')}catch(e){toast(e.message)}}
async function regenerateCredential(type){if(!confirm(`Regenerate ${type==='token'?'API token':'instance secret'}? The old credential will stop working.`))return;try{await api('/api/v1/account/api/regenerate',{method:'POST',body:JSON.stringify({type})});toast('Credential regenerated.');show('API Details')}catch(e){toast(e.message)}}
function security(){const enabled=localStorage.getItem('login_notifications')!=='0';app.innerHTML=`<div class="hero"><div><div class="eyebrow">PROTECTION</div><h1>Security</h1><p>Protect your merchant account and webhook integrations.</p></div></div><div class="settings-grid"><div class="form-card"><h2>Account security</h2><p class="subtext">Recommended protections for your account.</p><div class="health" style="margin-top:15px"><span class="health-dot big"></span><div><b>Security checks enabled</b><small>Use strong passwords and keep API secrets private.</small></div></div><div class="toggle-row"><span>Login notifications</span><button class="toggle ${enabled?'on':''}" onclick="toggleSecurity(this)"><i></i></button></div><div class="notice">Login notification preference is saved on this device. Backend authentication remains active regardless of this preference.</div></div><div class="form-card"><h2>Webhook signing</h2><p class="subtext">Verify X-Webhook-Signature on your backend before processing payment notifications.</p><div class="pill-row" style="margin-top:15px"><span class="badge success">HMAC-SHA256 ready</span></div><div class="input-like" style="margin-top:12px">Webhook Secret: ••••••••••••••••••••</div><p class="muted" style="margin-top:10px">The secret is generated per merchant account and is never exposed in page source.</p></div></div>`}
function toggleSecurity(el){el.classList.toggle('on');localStorage.setItem('login_notifications',el.classList.contains('on')?'1':'0');toast('Security preference saved.')} 
function apkPage(){app.innerHTML=`<div class="hero"><div><div class="eyebrow">DEVELOPER SETTING</div><h1>Merchant APK</h1><p>Merchant companion application resources.</p></div></div><div class="info-card"><h2>Merchant application</h2><p class="subtext">Use the published merchant application build supplied by your administrator. This page does not invent a download URL when no APK file is published in the repository.</p><div class="notice" style="margin-top:14px">No APK binary is currently published in this GitHub Pages project.</div></div>`}
function videoPage(){app.innerHTML=`<div class="hero"><div><div class="eyebrow">HELP</div><h1>Video For Merchant</h1><p>Step-by-step merchant setup guidance.</p></div></div><div class="info-card"><h2>Merchant setup</h2><p class="subtext">Connect a merchant, create a payment link and verify the first transaction from the dashboard.</p><div class="actions"><button class="primary" onclick="show('Connect Merchant')">Connect Merchant</button><button class="secondary" onclick="show('Payment Link')">Create Payment Link</button></div></div>`}
function docsPage(){app.innerHTML=`<div class="hero"><div><div class="eyebrow">DEVELOPER SETTING</div><h1>Documentation</h1><p>API integration reference for your merchant server.</p></div></div><div class="settings-grid"><div class="form-card"><h2>Create Order</h2><p class="subtext">Send your API token as a Bearer token to the public order endpoint.</p><pre class="code-box">POST /api/create-order\nAuthorization: Bearer YOUR_API_TOKEN\nContent-Type: application/json\n\n{\n  "amount": 499,\n  "merchant_id": "MERCHANT_ID",\n  "customer_mobile": "9876543210",\n  "remark1": "Order 1001"\n}</pre></div><div class="form-card"><h2>Check Order Status</h2><p class="subtext">Query the status of an order created with your account.</p><pre class="code-box">POST /api/check-order-status\nAuthorization: Bearer YOUR_API_TOKEN\n\n{\n  "order_id": "ORDER_ID"\n}</pre></div></div>`}
async function supportPage(){const d=await api('/api/v1/support/tickets');state.tickets=d.tickets||[];app.innerHTML=`<div class="hero"><div><div class="eyebrow">SUPPORT</div><h1>Support Ticket</h1><p>Create a support request and continue the conversation from your dashboard.</p></div><button class="primary" onclick="openTicketModal()">＋ New Ticket</button></div><div class="table-panel"><div class="panel-head"><div><h2>Your tickets</h2><p>${state.tickets.length} ticket${state.tickets.length===1?'':'s'}</p></div><button class="secondary" onclick="show('Support Ticket')">Refresh</button></div><div class="support-list">${state.tickets.length?state.tickets.map((t,i)=>`<div class="ticket" onclick="openTicket(${i})"><div class="ticket-top"><b>${esc(t.subject)}</b>${statusBadge(t.status)}</div><small>#${esc(t.ticketId)} · ${esc(t.category)} · ${esc(t.priority)} · ${fmtDate(t.lastMessageAt)}</small></div>`).join(''):empty('No support tickets','Create a ticket if you need help with payments, API, account or technical issues.')}</div></div>`}
function openTicketModal(){document.body.insertAdjacentHTML('beforeend',`<div class="modal show" id="ticketModal"><div class="modal-card"><button class="modal-close" onclick="closeModal('ticketModal')">×</button><div class="eyebrow">SUPPORT</div><h2>New Support Ticket</h2><div class="form-grid"><div class="field full"><label>Subject</label><input id="tSubject" maxlength="160" placeholder="Describe the issue"></div><div class="field"><label>Category</label><select id="tCategory"><option value="payment">Payment</option><option value="api">API</option><option value="kyc">KYC</option><option value="account">Account</option><option value="technical">Technical</option><option value="other">Other</option></select></div><div class="field"><label>Priority</label><select id="tPriority"><option value="low">Low</option><option value="normal" selected>Normal</option><option value="high">High</option><option value="urgent">Urgent</option></select></div><div class="field full"><label>Message</label><textarea id="tMessage" placeholder="Explain the issue"></textarea></div></div><div class="actions"><button class="primary" onclick="createTicket()">Create Ticket</button><button class="secondary" onclick="closeModal('ticketModal')">Cancel</button></div></div></div>`)}
async function createTicket(){const subject=document.getElementById('tSubject').value.trim(),message=document.getElementById('tMessage').value.trim(),category=document.getElementById('tCategory').value,priority=document.getElementById('tPriority').value;if(!subject||!message)return toast('Subject and message are required.');try{await api('/api/v1/support/tickets',{method:'POST',body:JSON.stringify({subject,message,category,priority})});closeModal('ticketModal');toast('Support ticket created.');show('Support Ticket')}catch(e){toast(e.message)}}
function openTicket(i){const t=state.tickets[i];openInfoModal('Ticket #'+t.ticketId,`<p><b>${esc(t.subject)}</b></p><div class="pill-row">${statusBadge(t.status)}<span class="pill">${esc(t.category)}</span><span class="pill">${esc(t.priority)}</span></div><div style="margin-top:15px">${(t.messages||[]).map(m=>`<div class="notice" style="margin:8px 0"><b>${esc(m.sender)}</b><br>${esc(m.text)}<br><small>${fmtDate(m.createdAt)}</small></div>`).join('')}</div><div class="actions"><button class="secondary" onclick="closeModal('infoModal')">Close</button></div>`)}
function faqPage(){app.innerHTML=`<div class="hero"><div><div class="eyebrow">HELP CENTER</div><h1>FAQ</h1><p>Answers to common AutoGateway merchant questions.</p></div></div><div class="info-card"><div class="faq"><b>How do I create a payment link?</b><p>Connect an active merchant, open Payment Link, enter the amount and create the link. The backend returns a unique checkout URL.</p></div><div class="faq"><b>Where are API credentials?</b><p>Open API Details. Your token and instance secret are generated for your authenticated merchant account.</p></div><div class="faq"><b>How do I receive payment events?</b><p>Save an HTTPS webhook endpoint in API Details and verify the webhook signature on your server.</p></div><div class="faq"><b>How can I change my password?</b><p>Use Change Password and provide your current password and a new password of at least 8 characters.</p></div><div class="faq"><b>Where can I see transaction status?</b><p>Open Transactions to view orders, amounts, statuses and timestamps returned by the backend.</p></div></div>`}
function sdkPage(){app.innerHTML=`<div class="hero"><div><div class="eyebrow">DEVELOPER SETTING</div><h1>SDK File</h1><p>Integration resources for your merchant server.</p></div></div><div class="info-card"><h2>API-first integration</h2><p class="subtext">The current project exposes the HTTP API directly. Use the Documentation page for request examples and API Details for your account credentials.</p><div class="actions"><button class="primary" onclick="show('Documentation')">Open Documentation</button><button class="secondary" onclick="show('API Details')">Open API Details</button></div></div>`}
async function updatePlan(){try{const d=await api('/api/v1/subscriptions/me');const p=d.plan;document.getElementById('planName').textContent=p?.name||'Free Trial';document.getElementById('planStatus').textContent=d.active?'Active':d.expired?'Expired':'Trial';document.getElementById('planExpiry').textContent=d.expiresAt?new Date(d.expiresAt).toLocaleDateString('en-IN'):'2 days';document.getElementById('planDays').textContent=d.expiresAt?Math.max(0,Math.ceil((new Date(d.expiresAt)-Date.now())/86400000))+' days':'2 days';document.getElementById('planBar').style.width=d.active?'100%':'55%'}catch{}}
function closeModal(id){document.getElementById(id)?.remove()}function openInfoModal(title,body){document.getElementById('infoModal')?.remove();document.body.insertAdjacentHTML('beforeend',`<div class="modal show" id="infoModal"><div class="modal-card"><button class="modal-close" onclick="closeModal('infoModal')">×</button><h2>${esc(title)}</h2>${body}</div></div>`)}
function copyText(v){return navigator.clipboard?.writeText(v).then(()=>toast('Copied to clipboard.')).catch(()=>toast('Copy failed.'))}
window.copyText=copyText;window.closeModal=closeModal;window.show=show;window.openMerchantModal=openMerchantModal;window.saveMerchant=saveMerchant;window.merchantInfo=merchantInfo;window.openLinkModal=openLinkModal;window.createRealLink=createRealLink;window.filterTx=filterTx;window.saveCheckout=saveCheckout;window.buyPlan=buyPlan;window.saveAccount=saveAccount;window.changePassword=changePassword;window.loadApiDetails=loadApiDetails;window.saveWebhook=saveWebhook;window.regenerateCredential=regenerateCredential;window.toggleSecurity=toggleSecurity;window.openTicketModal=openTicketModal;window.createTicket=createTicket;window.openTicket=openTicket;
loadAccount().then(()=>show('Dashboard')).catch(()=>{localStorage.removeItem('autogateway_token');localStorage.removeItem('autogateway_user');location.href='./index.html?session=expired'});
